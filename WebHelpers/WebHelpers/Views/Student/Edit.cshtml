@model WebHelpers.Models.StudentViewModel

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Editar Estudiante</h2>
@using (Html.BeginForm("Edit", "Student"))
{
    <table>
        <tbody>
            <tr>
                <td width="90px" height="40px">@Html.LabelFor(m => m.student.Id)</td>
                <td>@Html.TextBoxFor(m => m.student.Id, new { @class = "form-control" })</td>
            </tr>
            <tr>
                <td width="90px" height="40px">@Html.LabelFor(m => m.student.Nombre)</td>
                <td>@Html.TextBoxFor(m => m.student.Nombre, new { @class = "form-control" })</td>
            </tr>
            <tr>
                <td width="90px" height="40px">@Html.LabelFor(m => m.student.Edad)</td>
                <td>@Html.TextBoxFor(m => m.student.Edad, new { @class = "form-control" })</td>
            </tr>
            <tr>
                <td width="90px" height="40px">@Html.LabelFor(m => m.student.CurseId)</td>
                <td>
                    @Html.DropDownListFor(m => m.student.CurseId, new SelectList(Model.Cursos, "Id", "Nombre"))
                </td>
            </tr>
        </tbody>
    </table>
    <input type="submit" value="Guardar" />
}


